# Exemplo de configuração de ambiente. Copie para .env e ajuste.
# Use %USERPROFILE% para basear no diretório do usuário atual.
# BASE_DIR=%USERPROFILE%plataforma-votacao # Opcional, se seu código precisar disso

# Configuração do Banco de Dados PostgreSQL Local (PADRÃO)
# Certifique-se de criar um banco chamado 'plataforma' com usuário 'postgres' e a senha definida abaixo
DATABASE_URL=postgresql://postgres:kgb123!@localhost:5432/plataforma